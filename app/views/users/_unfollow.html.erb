<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>

<%= button_tag(type: 'submit', class: "waves-effect waves-light btn red darken-2 white-text followbutton") do %>
 <i class="material-icons left">notifications</i>Following

<% end %>

<% end %>